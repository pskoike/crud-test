<h1>POSTS</h1>
<ul>
  <% @posts.each do |post| %>
    <li>
      <%= post.title %>
      <br>
      <%= post.content %> 
    </li>
    <%= link_to 'edit', edit_post_path(post.id) %>
    <hr>
  <% end %>
</ul>

